import React from 'react';

const ConstraintChecker = ({ grid }) => {
    // Add logic to check crossword constraints here
    return (
        <div className="text-sm text-gray-600">
            {/* Output validation messages here */}
            Grid is valid ✅
        </div>
    );
};

export default ConstraintChecker;
